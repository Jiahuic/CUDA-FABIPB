build/
build-*/
# The manuscript is deliberately NOT version-controlled: it must not be
# published with the code. Both locations are ignored and both were purged from
# git history. The files stay on disk; back them up out of band.
paper/
results/

