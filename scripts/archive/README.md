# Archived Scripts

Scripts in this directory are retained for reproducibility but are not part of
the active workflow.

`run_hybrid_matrix.sh` was a standalone hybrid-only benchmark runner. Its
functionality is covered by the `hybrid_best` configuration in
`scripts/run_benchmark_matrix.sh`.
